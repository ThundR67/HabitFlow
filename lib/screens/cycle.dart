import 'package:flutter/material.dart';

/// Shows data about a cycle.
class CycleInfo extends StatelessWidget {
  /// Constructs.
  const CycleInfo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Under Dev'),
    );
  }
}
